# Please refer https://aka.ms/HTTPSinContainer on how to setup an https developer certificate for your ASP .NET Core service.

version: '3.4'

services:
  ms-sql-server:
    image: sqlserver
    build:
      dockerfile: docker/sql-server/Dockerfile
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: Enterpasswordhere123#
      MSSQL_PID: Express
    ports:
      - 1000:1433
  pxlprw2021team08api:
    image: pxlprw2021team08api
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    ports:
      - 5000:5000