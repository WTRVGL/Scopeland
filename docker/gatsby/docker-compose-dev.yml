version: "3.7"

services:
  gatsby-dev:
    volumes:
      - ./gatsby_client/src:/app/src
    build:
      context: ../../
      dockerfile: docker/gatsby/Dockerfile.dev
    ports:
      - "8000:8000"
volumes:
  src: